# Features

Each product feature should own its components, hooks, API functions, schemas, and tests. Features may import shared UI and infrastructure, but should not import another feature's internals.

